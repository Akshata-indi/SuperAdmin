// componentMap.js
import LogoConfig from '../../configuration/LogoConfig';
import Profile from '../../configuration/Profile';
import Search from '../../configuration/Search';

const componentMap = {
  LogoConfig,
  Profile,
  Search,
};

export default componentMap;
