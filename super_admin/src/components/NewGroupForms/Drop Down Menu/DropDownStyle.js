const dropDownStyles = {
    select: {
        base: "block appearance-none w-full bg-white border border-gray-300 text-gray-700 py-2 mx-4  rounded-md leading-tight focus:outline-none focus:ring focus:ring-indigo-200",
        placeholder: "text-gray-400",
        border: "border-gray-300",
        flex: "flex-1", // Adjust flex property for the dropdown width
    },
    addButton: {
        base: "bg-blue-500 text-white px-8 py-2 mx-2 text-sm rounded-md",
        ml: "", // Adjust ml (margin-left) for the button position
    },
};

export default dropDownStyles;
